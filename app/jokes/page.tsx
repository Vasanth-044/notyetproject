'use client';\n\nimport { useState } from 'react';\n\nexport default function JokesPage() {\n  const [joke, setJoke] = useState<{ setup?: string; delivery?: string; joke?: string } | null>(null);\n  const [loading, setLoading] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n\n  const fetchJoke = async () => {\n    setLoading(true);\n    setError(null);\n    try {\n      const response = await fetch('https://official-joke-api.appspot.com/random_joke');\n      if (!response.ok) throw new Error('Failed to fetch joke');\n      const data = await response.json();\n      setJoke(data);\n    } catch (err) {\n      setError(err instanceof Error ? err.message : 'An error occurred');\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center p-4\">\n      <div className=\"bg-white rounded-lg shadow-2xl p-8 max-w-2xl w-full\">\n        <h1 className=\"text-4xl font-bold text-center text-gray-800 mb-8\">😂 Joke Generator</h1>\n        \n        <button\n          onClick={fetchJoke}\n          disabled={loading}\n          className=\"w-full bg-blue-600 hover:bg-blue-700 disabled:bg-gray-400 text-white font-bold py-3 px-6 rounded-lg transition duration-200 mb-6\"\n        >\n          {loading ? 'Loading...' : 'Get a Random Joke'}\n        </button>\n\n        {error && (\n          <div className=\"bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4\">\n            {error}\n          </div>\n        )}\n\n        {joke && (\n          <div className=\"bg-gray-50 border-l-4 border-blue-600 p-6 rounded\">\n            {joke.setup ? (\n              <>\n                <p className=\"text-lg text-gray-800 font-semibold mb-3\">{joke.setup}</p>\n                <p className=\"text-lg text-gray-600 italic\">{joke.delivery}</p>\n              </>\n            ) : (\n              <p className=\"text-lg text-gray-800\">{joke.joke}</p>\n            )}\n            <p className=\"text-sm text-gray-500 mt-4\">Type: {joke.setup ? 'Two-part' : 'Single-part'}</p>\n          </div>\n        )}\n\n        {!joke && !error && (\n          <div className=\"text-center text-gray-500\">\n            <p>Click the button to generate a random joke!</p>\n          </div>\n        )}\n      </div>\n    </div>\n  );\n}\n